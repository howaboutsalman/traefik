# Traefik Dashboard Domain
TRAEFIK_DOMAIN=traefik.example.com

# Traefik Basic Authentication
# Username for Traefik dashboard
TRAEFIK_USERNAME=admin

# Hashed password for Traefik dashboard
# Plain password: your_secure_password
# Generate with: echo $(htpasswd -nb username password) | sed -e s/\\$/\\$\\$/g
# Or: echo $(openssl passwd -apr1 yourpassword) | sed -e s/\\$/\\$\\$/g
TRAEFIK_HASHED_PASSWORD=$$apr1$$xxxxxxxx$$xxxxxxxxxxxxxxxxxxxxxxxx

# Let's Encrypt ACME Email
# Email address for Let's Encrypt certificate notifications
ACME_EMAIL=admin@example.com

# PostgreSQL Database Configuration
# Username for database access
DB_USER=dbadmin
# Strong password for database (avoid $ symbol or escape it as $$)
DB_PASSWORD=YourSecurePassword123!
# Name of the database to create
DB_NAME=myappdb

# pgAdmin Configuration (web-based database management)
# Domain for pgAdmin web interface
PGADMIN_DOMAIN=pgadmin.example.com
# Email for pgAdmin login
PGADMIN_EMAIL=admin@example.com
# Password for pgAdmin login
PGADMIN_PASSWORD=YourPgAdminPassword123!